# Legacy Render Reference

Legacy render backend entrypoints:

- `apps/cards-api/main.py`
- `apps/cards-worker/main.py`

Trainer static entrypoint:

- `apps/trainer/index.html`

The trainer path does not use `/api`; queue/video rendering stays in legacy backend scope.
